This change breaks 2 policy checks

This change breaks 2 policy checks

QA pass: UTM Grabber v3 text deny — expected Deny, would be Allow
QA fail: Akismet must stay denied — expected Deny, would be Allow

 Save anyway, even though policy checks will fail

 Cancel